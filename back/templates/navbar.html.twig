
<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid d-flex ">
    {% if app.request.attributes.get('_route') == 'app_register' %}
      <a class="nav-link navbar-brand">REGISTER</a> 
    {% elseif app.request.attributes.get('_route') == 'app_login' %}
             <a class="nav-link  navbar-brand">LOGIN</a>
    {% else %}
      {% if app.user %}
         <a class="nav-link navbar-brand">{{ app.user.username|upper }} </a>
      {% endif %}

    {% endif %}
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <ul class="nav navbar-nav ">
      {% if app.request.attributes.get('_route') == 'app_register' %}
        <li class="nav-item">
           <a class="btn btn-secondary" href="{{ path('app_login') }}">Login?</a>
        </li>

      {% elseif app.request.attributes.get('_route') == 'app_login' %}
        <li class="nav-item">
           <a class="btn btn-secondary" href="{{ path('app_register') }}">Register?</a>
        </li>
      
      {% else %}
        <li style="margin-right:5px;" class="nav-item">
           <a class="nav-link showHistorique" data-bs-toggle="modal"   data-bs-target="#historiqueModal"  href="">Historique</a>
        </li>
        <li class="nav-item">
           <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
        </li>
      
      {% endif %}

      </ul>
    </div>
  </div>
</nav>