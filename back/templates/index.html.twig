  {% extends "base.html.twig" %}

   {% block body %} 
       
<div class="">

{% if app.user %}
                                                           
    <div class="container-fluid">
    <h1>
    TOKY NOTE
    </h1>
    <br><br><br><br><br><br><br><br>
    
    <div class="list">

    <div class="row">
          <div class="col-md-8">
    
    <form  class="form-inline" method="POST" action="{{path('store.to_do')}}"> 
   
    <input type="text" name="to_do" id="myinput" placeholder="to_do" />

    <button type="submit" class="btn btn-success">to_do</button>

    </form>  
    </div>
          </div>
    <table>
    {%for todolists in todolist%}
    <tr>
    
    <td><a href="{{path('delete.to_do', {'id': todolists.id})}}"  class="btn btn-default">{{todolists.todo}}</a></td>
    <td><a href="{{path('update.to_do', {todo: todolists.todo, id: todolists.id})}}"  class="btn btn-default">finish</a></td>
    <td><a href="{{path('delete.to_do', {'id': todolists.id})}}"  class="btn btn-default">delete</a></td>
    </tr>{%endfor%}
    </table>
    </div>
  
    <h1>
    tache fait
    </h1>
    <ul>
    {%for dids in did%}
    <li>{{dids.title}}
    </li>
    {%endfor%}
    </ul>

    </div>
                        {% else %}
                                <div>
                                        <a class="btn btn-sm btn-primary" href="{{ path('app_register') }}">S'inscrire</a>
                                        <a class="btn btn-sm btn-success" href="{{ path('app_login') }}">Se connecter</a>
                                </div>
                        {% endif %}
                </div>
                <div class="container">
                        {% if message is defined %}
                                <div class="alert alert-danger">
                                        {{ message }}
                                </div>
                        {% endif %}
                     
                </div>
                {% endblock %}
                {% block javascripts %}
                        <script
                                src="https://code.jquery.com/jquery-3.5.1.min.js"
                                integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
                        crossorigin="anonymous">
                        </script>
                        <script src="/js/script.js"></script>
                {% endblock %}
  
